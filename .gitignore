# large data & generated outputs (never commit)
# raw videos, session outputs, caches (16GB+)
data/
# trained YOLO weights + OpenVINO export
runs/
# auto-downloaded pose .task models
models/
# auto-labeled training frames (regenerable)
dataset_ball/

# model weights / media / archives
*.pt
*.task
*.onnx
*_openvino_model/
*.mp4
*.mov
*.avi
*.zip

# python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# generated native-scale training set (regenerate: tools/make_dataset_native.py)
dataset_ball_native/
